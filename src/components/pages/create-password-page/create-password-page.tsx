import { CreatePassword } from '@/components/forms'
import { Page } from '@/components/layouts'

export const CreatePasswordPage = () => {
  return (
    <Page>
      <CreatePassword onSubmit={() => {}} />
    </Page>
  )
}
